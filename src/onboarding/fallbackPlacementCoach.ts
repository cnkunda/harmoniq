/** Mirrors `FALLBACK_ONBOARDING_PLACEMENT` when the API is unreachable. */
export const FALLBACK_PLACEMENT_COACH_COPY =
  'Your placement takes give us a usable baseline for pitch, phrasing, and timing. What usually opens up first is a little more air between ideas — micro-pauses make the next note feel intentional instead of rushed. Harmoniq will weight suggestions from here; when you add a real song, the home card will line up drills with what you are working on. Keep bends relaxed and let vibrato settle before you move on — small stability wins there carry into everything else.'
