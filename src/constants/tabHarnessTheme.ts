import type { TabThemeColors } from '@/types/tabMessage'

/** README / design palette embedded into the harness via `setTheme` (matches `index.html` defaults). */
export const TAB_HARNESS_THEME: TabThemeColors = {
  mainGlyphColor: '#F0DEB4',
  secondaryGlyphColor: '#D4A574',
  barSeparatorColor: '#5C4535',
  scoreInfoColor: '#A08060',
  staffLineColor: '#5C4535',
  barNumberColor: '#8B7D6B',
}
