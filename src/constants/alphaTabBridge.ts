/**
 * Minimum ms between `noteEvent` bridge posts — MANUAL_QA (~33 Hz cap). 1000/31 is about 32.3 Hz.
 * Native harness: `NOTE_EVENT_MIN_MS` in assets/alphatab-harness/index.html must match.
 */
export const ALPHA_TAB_NOTE_EVENT_MIN_INTERVAL_MS = 31
