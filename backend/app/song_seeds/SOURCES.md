# Song seed candidates (commit 68)

These strings are **curated practice titles** for curriculum / taste suggestion copy — not a licensed catalog API.

- They are generic **“Artist — Song”** style labels used only as **ordered text candidates** for `TasteProfile.song_candidates`.
- They are **not** guaranteed to match YouTube uploads or Harmoniq’s analyzed library; real routing still uses analyzed `job_id`s from the client.
- Revise this list as you expand per-style pedagogy; keep at least **three** entries per supported `style_label` (see `__init__.py`).
